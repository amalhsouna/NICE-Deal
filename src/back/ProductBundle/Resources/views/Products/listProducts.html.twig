{% extends "::baseAdmin.html.twig" %}
{% block body %}
    {% include '::includes/topColumn.html.twig' %}
    {% include '::includes/leftColumn.html.twig' %}
     <div class="modal fade" id="confirm-delete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title" id="myModalLabel">Confirmer la suppression</h4>
                </div>
            
                <div class="modal-body">
                    <p>Vous êtes sur le point de supprimer une piste, cette procédure est irréversible.</p>
                    <p>Voulez-vous poursuivre?</p>
                    <p class="debug-url"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                    <a class="btn btn-danger btn-ok">Supprimer</a>
                </div>
            </div>
        </div>
    </div>
     
    <h1>Produits<small>Shop</small></h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i>Accueil</a></li>
        <li class="active">Produits</li>
    </ol>
    </section>
    <section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header with-border">
                  <h3 class="box-title">Mon Stocke de Produits</h3>
                  <div class="box-tools pull-right">
                    <button class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
                  </div>
                </div>
    <div class="box-body">
       <div class="table-responsive">
        <div class="row">
            <div class="col-md-12">
                <div class="box box-danger">
                    <div class="box-header with-border">
                          <h3 class="box-title">Liste des Produits</h3>
                          <div class="box-tools pull-right">
                            <button class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
                          </div>
                    </div>
        <div class="box-body">
            <div class="col-lg-3">
            <div class="input-group"> <span class="input-group-addon">Filter</span>
                <input id="filter" type="text" class="form-control" placeholder="Recherche ici...">
            </div>
            </div>
            <div class="input-group pull-right">
                <a title="" data-toggle="tooltip" data-placement="bottom" href="{{path('back_product_admin_add_products')}}" class="btn btn-success pull-right" data-original-title="Créer un nouveau Produit"><span></span><i class="glyphicon glyphicon-plus"></i></a>
            </div>
           
            <table class="ProdTable table table-bordered table-striped table-hover no-margin tablet no-paging footable-loaded footable">
            <thead>
                <tr>
                  <th class="footable-visible footable-first-column footable-sortable">Nom de produit<span class="footable-sort-indicator"></span></th>
                  <th class="footable-visible footable-first-column footable-sortable">Prix vente</th>
                  <th class="footable-visible footable-first-column footable-sortable">Description</th>
                  <th class="footable-visible footable-first-column footable-sortable">Partenaire</th>
                  <th class="footable-visible footable-last-column footable-sortable footable-sorted">Outiles</th>
                </tr>
            </thead>
             <tbody class="searchable">
                {% for product in products %}
                <tr>
                    <td class="footable-visible footable-first-column">{{product.name}}</td>
                    <td class="footable-visible">{{product.price}}</td>
                    <td class="footable-visible">{{product.description|slice(0, 80)|raw}} ....</td>
                    <td class="footable-visible footable-first-column">{{product.partenary.name}}</td>
                    <td class="footable-visible footable-last-column" style="width: 10%">
                        <a href="{{path('back_product_admin_details_products',{'id': product.id})}}" class="viewt view label label-info" aria-hidden="true"><span class="fa fa-eye"> </span></span>
                        <a href="{{path('back_product_admin_put_products',{'id': product.id})}}" class="viewt view label label-warning" aria-hidden="true"><span class="fa fa-edit"> </span></span>
                        <a href="#" data-href="{{path('back_product_admin_delete_products',{'id': product.id})}}" data-toggle="modal" data-target="#confirm-delete"></a>
                        <button class="label-danger" data-href="{{path('back_product_admin_delete_products',{'id': product.id})}}" data-toggle="modal" data-target="#confirm-delete"><span class="fa fa-remove"></span></button>
                    </td>
                </tr>
                {% endfor %}
            </table>
             <nav>
            <ul class="pagination">
                {{ knp_pagination_render(paging) }}
            </ul>
            </nav>
      
    </div>
   </div></div>
    </div>
    </section>
        
  {% include '::includes/footer.html.twig' %}
{% endblock %}