{% extends "::baseAdmin.html.twig" %}
{% block body %}
    {% include '::includes/topColumn.html.twig' %}
    {% include '::includes/leftColumn.html.twig' %}
     <div class="modal fade" id="confirm-delete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
            
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title" id="myModalLabel">Confirmer la suppression</h4>
                </div>
            
                <div class="modal-body">
                    <p>Vous êtes sur le point de supprimer une piste, cette procédure est irréversible.</p>
                    <p>Voulez-vous poursuivre?</p>
                    <p class="debug-url"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                    <a class="btn btn-danger btn-ok">Supprimer</a>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <h3>Liste des produits</h3>

        <table class="table table-bordered">
        <thead>
            <tr>
              <th>Nom de produit</th>
              <th>Prix</th>
              <th>Description</th>
              <th>Modifier</th>
              <th>Supprimer</th>
            </tr>
        </thead>
         <tbody>
            {% for product in products %}
            <tr>
                <td>{{product.name}}</td>
                <td>{{product.price}}</td>
                <td>{{product.description}}</td>
                <td><a href="{{path('back_product_admin_put_products',{'id': product.id})}}" class="glyphicon glyphicon-pencil" aria-hidden="true"></span></td>
                <td>
                    <a href="#" data-href="{{path('back_product_admin_delete_products',{'id': product.id})}}" data-toggle="modal" data-target="#confirm-delete"></a><br>
                    <button class="glyphicon glyphicon-remove" data-href="{{path('back_product_admin_delete_products',{'id': product.id})}}" data-toggle="modal" data-target="#confirm-delete"></button>
                </td>
            </tr>
            {% endfor %}
        </table>
         <nav>
        <ul class="pagination">
            {{ knp_pagination_render(paging) }}
        </ul>
        </nav>
    </div>
        
  {% include '::includes/footer.html.twig' %}
{% endblock %}